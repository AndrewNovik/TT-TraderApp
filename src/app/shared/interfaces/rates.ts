export interface ExcahgeRate {
    lastPrice: string;
    lowPrice24h: string;
    prevPrice24h: string;
    symbol: string;
    turnover24h: string;
    volume24h: string;
    highPrice24h: string;
}